# 2026-07-21

## Notification 模块设计文档（交付物）
- 产出 `docs/notification-module-design.md`：完整设计稿（SSE 实时推送 + 应用内通知中心 + 通知偏好设置 + 邮件系统），严格对齐本仓库约定（六边形/ CQRS / 事件驱动 / DDD / Fx / chi / NATS / sqlc / goose）。
- **经代码核查确认的两点决策**（已写入文档，非假设）：
  - SSE 鉴权：`authn.RequireAuth` 仅解析 `Authorization` 头（`internal/shared/modules/authn/middleware.go`），浏览器 `EventSource` 不能带请求头 → SSE 端点改用 `?token=`（回退 `Authorization`），复用注入的 `authn.TokenVerifier` + `authn.WithClaims`。`TokenVerifier` 已在 fx 图按接口提供（`users/module.go` 的 `fx.As(new(authn.TokenVerifier))`），handler 直接注入即可。
  - 出价超越通知：`auction/domain/event/bid_placed_event.go` 的 `BidPlacedEvent` 仅含 `BidID/AuctionID/UserID`(新出价者)/`Amount`，**无上一位出价者** → "被超越"通知须走只读 `AuctionReadPort`（`SourceEventResolverPort`）解析上一手最高出价用户，不修改 auction 模块。
- **架构要点**：Notification 为纯消费者，订阅既有流 `AUCTION_EVENTS`/`DEPOSIT_EVENTS`/`PAYMENT_EVENTS`/`LISTING_EVENTS`（只读，不改生产方）；新增 `NOTIFICATION_EVENTS` 流（`notification.evt.>`）承载内部事件。`notifications` 表为站内中心权威源（幂等键 `source_event_id:user_id:channel`）；SSE Hub 复用 `deposit/infra/websocket/deposit_hub.go` 的 `UserSubscriberRegistry`+`EventConsumer` 按 userID 扇出（queue group 支持水平扩容）；邮件经 `EmailPort` 抽象 + `SMTPEmailAdapter` 默认实现，outbox + Relay 保证至少一次且不重复（对齐 payment 模块）。迁移接续 `000021` → `000022/23/24`。
- 待确认仅剩：邮件默认开关范围（建议 payment/deposit/system 默认开，其余关）。实施分 6 期（脚手架→中心读写→SSE→事件订阅编排→邮件→验证门禁）。

## Notification 模块「仅站内」实施完成（#92–#100，门禁全绿）
- 用户决策「先仅站内开始实施」：只做 SSE 推送 + 通知中心 + 偏好，**暂缓邮件**（无 `EmailPort`/`SMTPEmailAdapter`/`EmailDispatchConsumer`/`notification.evt.email.*`）；`notification_outbox` 保留用于可靠发布 `notification.evt.created` 驱动 SSE。
- **落地范围**：迁移 `000022/23/24`（notifications / notification_preferences / notification_outbox）；域层 enum（type/category/channel，struct `XxxEnum` + **指针接收者 `String()`**，调用前须先赋值本地变量否则编译错）+ model（NotificationModel / NotificationPreferenceModel，`DefaultPreferences()` 四类目 in_app 默认开）+ errs + event；应用层 5 命令（create/mark-read/mark-all-read/delete/update-preferences）+ 3 查询（list/unread-count/get-preferences）+ `NotificationApplicationService`（编排）+ `SourceEventConsumer`（订阅）；基础设施 repository/mapper/uow/outbox-relay/sse/messaging/http。
- **SSE 扇出**：`SubscriberRegistry`（mutex map[userID]set）+ `RealtimeHub.Run` 消费 JetStream，从 subject `notification.evt.{userID}` token[2] 取 userID 后 `PublishToUser`；`Client.send` 有缓冲、满即丢弃不阻塞扇出。SSE 端点用 `?token=`（EventSource 不能带头），注入 `authn.TokenVerifier`，路由**不挂** RequireAuth。
- **事件来源映射（已核查 wire 形状）**：payment 扁平（`payment.evt.deposit.success`：event_id/payment_id/user_id/amount_in_cents/currency）；deposit/auction 双层信封（Envelope{event_type,event_id,...,data} + 内层 Data）。`bid_placed` 仅带新出价者 → 「被超越」经只读 `AuctionReadPort.FindPreviousHighestBidderID`（查共享 `bids` 表，命中 `idx_bids_auction_id_amount`）；`auction_ended` 赢家经 `FindBidderIDByBidID`。listing 事件无 userID **暂缓**；withdrawal.completed/.failed payment 尚未发布 **暂休眠**。消费者沿用仓库惯例 **AckNonePolicy+DeliverNewPolicy 临时消费者**（漏收可经 REST 恢复），非设计文档建议的 durable+显式 ack。
- **幂等**：notifications idempotency_key = `source_event_id:user_id:in_app`；`InsertNotification` 冲突返回 `pgx.ErrNoRows` → repo `Save` 返回 `(model,false,nil)` → 命令跳过、**不写 outbox 不 Commit**。
- **Fx/cmd 装配**：`notification/module.go` 全 provider（含 `func(pool)(sqlcgen.DBTX){return pool}` 让 pool-backed 读仓 + auction_read 仓解析）+ 5 个 Register\*（OutboxRelay/Routes/StreamRoute/Hub/SourceConsumer）；`cmd/all.go` 与 `cmd/auction.go` 均加 `notification.Module` + 5 Register\*。
- **HTTP**：`/api/v1/notifications`（GET / list?unread&limit&offset、GET /unread-count、POST /read-all、POST /{id}/read、DELETE /{id}、GET|PUT /preferences，均挂 RequireAuth）+ `GET /api/v1/notifications/stream`（token query）。httperrs 新增 NOTIFICATION_01/02/03 + MapDomainError 映射。
- **单测（新增，全 PASS）**：preference_test（默认/禁用/未知类目回退/未知渠道）、create_notification_command_test（新行写 outbox+Commit / 重复跳过不 Commit / 非法类型）、list_notifications_query_test（limit 默认 20 / 上限 100 / 负 offset 归零 / unread 路由）、notification_application_service_test（偏好开→落库 / 偏好关→跳过不开事务 / 无上一手→跳过 / 有上一手→通知输家 / 无中标 bid→跳过 / 未知 deposit 事件→跳过）、sse hub_test（registry 扇出多端、Remove 停发、Run 路由到收件人、非法 subject 忽略）+ extractUserID 内部表驱动测试。
- **门禁全绿（2026-07-21）**：`go build ./...`=0；`go test ./...`=0；`golangci-lint run` 0 issues（修 `formatMoney` 的 mnd → 加常量 `centsPerUnit=100`）；`make nilaway`=0（`--exclude-pkgs` 已含 tests/mocks）。`make update-mocks` 生成 notification 系列 mock（NotificationRepository/PreferenceRepository/AuctionReadPort/NotificationUnitOfWork(Factory)/NotificationOutboxRepository），接口异名无文件冲突。
- **后续（未做）**：邮件系统整期；listing 通知需 follower 读模型；payment withdrawal 事件发布后接线 withdrawal_completed/failed；无真实 PG 的 handler/repository 集成测试（可 testcontainers 补）。

## Withdrawal 通知接线（后续项①，已完成，全绿）
- **决策**：三项后续中 withdrawal 通知范围最清晰（全在 payment+notification 内），优先做掉；listing 通知因项目**无任何关注/收藏模型**（migrations/ + 全模块 Grep 确认无 watch/follow/favorite/subscribe，README:1120 亦注明未实现）且 listing 事件是商品目录生命周期（spu/sku 上下架，payload 仅 spuID/skuID、无 auctionID，bids 表当隐式关注者语义对不上——新上架 SPU 无出价），实为新特性，**已暂缓待产品决策**；testcontainers 集成测试待确认。
- **payment 发布侧**：`domain/event/payment_event.go` 新增 `WithdrawalCompletedEvent`/`WithdrawalFailedEvent` + EventType（`SubjectWithdrawalCompleted/Failed` 早已定义未用）；`infra/event/envelope/envelope.go` 新增 `WithdrawalCompleted/FailedPayload`（**扁平**，含 withdrawal_id/user_id/amount_in_cents/currency/out_biz_no/alipay_order_id 或 fail_reason/occurred_at）+ `ToWithdrawalCompleted/FailedOutboxEvent`；`application/service/withdrawal_consumer.go` 的 `applyPayoutSaga` 在 `markWithdrawal` 后、`Complete` 前调新增 `writeOutcomeEvent`——**同一 Saga 事务内** `writeUoW.OutboxRepository().Save`（成功=transfer.AlipayOrderID，失败=transferErr.Error()），经 relay 可靠发布。stream `PAYMENT_EVENTS` subject 为 `payment.>`，天然容纳 `payment.evt.withdrawal.*`。
- **notification 消费侧**：`source_event_consumer.go` 对 `StreamPaymentEvents` 新增两个独立 subscribe（`payment.evt.withdrawal.completed`→`handleWithdrawalCompleted`、`.failed`→`handleWithdrawalFailed`，解码扁平 `withdrawalEventMessage`）；`notification_application_service.go` 新增 `HandleWithdrawalCompleted`/`HandleWithdrawalFailed`（走既有 `dispatch`，类型枚举 `EnumNotificationTypeWithdrawalCompleted/Failed` 早已存在并映射到 Payment 类目，故枚举无需改）。
- **测试**：扩 `notification_application_service_test.go`（completed 偏好开→落库/偏好关→跳过、failed 偏好开→落库）；改既有 `payment/.../withdrawal_consumer_test.go`——加 `outboxMock *MockPaymentOutboxRepository`，成功/失败两路各加 `OutboxRepository().Save`（MatchedBy 校验 Subject/EventType）期望。
- **门禁全绿**：`go build ./...`=0、`go test ./...`=0、`golangci-lint run`(payment+notification) 0 issues、`make nilaway`=0。

## Watchlist 关注/收藏 + listing 事件扇出通知（方案A，已完成，全绿）
- **决策**：用户选方案A（轻量：新建 watchlists 表 + 用户显式收藏 API + listing 事件按关注者扇出）。关注粒度选 **SPU（拍品）级**——SPU 事件 `aggregate_id` 即 spuID；SKU 事件 `data.spu_id` 即 spuID；统一按 spuID 查关注者。listing 事件为双层信封 `Envelope{event_type,event_id,schema_version,timestamp,aggregate_type,aggregate_id,data}`，subject 动态 `listing.evt.{aggregate_type}.{id}`，stream `LISTING_EVENTS` subject `listing.evt.>`。
- **迁移**：`migrations/000025_create_watchlists_table.sql`（`id BIGSERIAL PK, user_id, spu_id, created_at, UNIQUE(user_id,spu_id)` + 两索引）。sqlc notification 块 schema=migrations，故 watchlists/spus 表均在同包生成覆盖（沿用 bids.sql 跨模块读取先例）。
- **领域/端口**：`domain/errs/errs.go` 加 `ErrWatchUserRequired/ErrWatchSpuRequired/ErrWatchNotFound`；`domain/model/watchlist.go` `NewWatchlist`/`ReconstructWatchlist`（不可变值对象）；`ports/watchlist_repository.go`（`Save`幂等 upsert/`Remove`/`ListByUser`/`FindWatcherIDsBySpuID`）；`ports/listing_read_port.go`（`FindSpuTitleByID` 只读 spus 表，富化通知正文）。
- **仓库/应用层**：`infra/repository/watchlist_repository.go`（UpsertWatchlist `ON CONFLICT DO UPDATE SET user_id=EXCLUDED.user_id RETURNING *` 恒返回 1 行；DeleteWatchlist `:one` 0 行→`ErrWatchNotFound`；`FindWatcherIDsBySpuID`）、`listing_read_repository.go`；`application/command` `CreateWatchCommand`（upsert）、`DeleteWatchCommand`；`application/query` `ListMyWatchesQuery`（复用 defaultPageSize=20/maxPageSize=100 分页钳制）。
- **HTTP**：`infra/http/dto/dto.go` 加 `WatchResponse/WatchListResponse/CreateWatchRequest`；`handler/watchlist_handler.go`（`CreateWatch` POST / `ListMyWatches` GET / `DeleteWatch` DELETE /{spuId}，均 RequireAuth 取 userID；同包复用 `parseIntQuery`/`parseIDParam`，新增 `parseSpuIDParam`）；`router` 加 `RegisterWatchlistRoutes`（`/api/v1/watchlists`）。
- **消费扇出**：`source_event_consumer.go` 新增订阅 `StreamListingEvents`→`handleListing`，解码 `listingEnvelope`，`resolveListingSpuID`（spu 取 aggregate_id / sku 取 data.spu_id），调 `service.HandleListingEvent`；`notification_application_service.go` 扩构造器加 `watchlist ports.WatchlistRepository`+`listingRead ports.ListingReadPort`，新增 `HandleListingEvent`（`resolveListingNotificationType` 将 `listing.spu/sku.published`→`EnumNotificationTypeListingPublished`、`*.off_shelf`→`EnumNotificationTypeListingOffShelf`，二者枚举早已存在并映射 `listing` 类目；查 spu 标题富化；`FindWatcherIDsBySpuID` 后逐个 `dispatch`，偏好检查+幂等键 `sourceEventID:userID:in_app`；无关注者→跳过）。
- **接线**：`module.go` 提供 `WatchlistRepository`/`ListingReadPort`（fx.As）、3 命令/查询、`WatchlistHandler`、`RegisterWatchlistRoutes`；`cmd/all.go`+`cmd/auction.go` 的 `fx.Invoke` 加 `notification.RegisterWatchlistRoutes`。
- **测试**：`make update-mocks` 生成 `MockWatchlistRepository`/`MockListingReadPort`；新增 `create_watch_command_test`/`delete_watch_command_test`/`list_my_watches_query_test`（分页钳制）、`notification_application_service_test.go` 扩 `HandleListingEvent`（未知类型跳过/无关注者跳过/发布通知所有关注者/SKU 下架通知），`source_event_consumer_internal_test.go`（`resolveListingSpuID` 表驱动）；改 `notification_application_service_test.go` 构造器注入两新 mock。顺手修 `parseIntQuery` 去掉恒为 0 的 `fallback` 形参（unparam）。
- **门禁全绿**：`go build ./...`=0、`go test ./...`=0、`golangci-lint run`(notification+cmd+httperrs) 0 issues、`make nilaway`=0。

## Watchlist 关注/收藏 + listing 事件扇出通知（方案A，已完成，全绿）
- **决策**：用户选方案A（轻量：新建 watchlists 表 + 用户显式收藏 API + listing 事件按关注者扇出）。关注粒度选 **SPU（拍品）级**——SPU 事件  即 spuID；SKU 事件  即 spuID；统一按 spuID 查关注者。listing 事件为双层信封 ，subject 动态 ，stream  subject 。
- **迁移**：（ + 两索引）。sqlc notification 块 schema=migrations，故 watchlists/spus 表均在同包生成覆盖（沿用 bids.sql 跨模块读取先例）。
- **领域/端口**： 加 ； /（不可变值对象）；（幂等 upsert///）；（ 只读 spus 表，富化通知正文）。
- **仓库/应用层**：（UpsertWatchlist  恒返回 1 行；DeleteWatchlist  0 行→；）、； （upsert）、； （复用 defaultPageSize=20/maxPageSize=100 分页钳制）。
- **HTTP**： 加 ；（ POST /  GET /  DELETE /{spuId}，均 RequireAuth 取 userID；同包复用 /，新增 ）； 加 （）。
- **消费扇出**： 新增订阅 →，解码 ，（spu 取 aggregate_id / sku 取 data.spu_id），调 ； 扩构造器加 +，新增 （ 将 →、→，二者枚举早已存在并映射  类目；查 spu 标题富化； 后逐个 ，偏好检查+幂等键 ；无关注者→跳过）。
- **接线**： 提供 /（fx.As）、3 命令/查询、、；+ 的  加 。
- **测试**： 生成 /；新增 //（分页钳制）、 扩 （未知类型跳过/无关注者跳过/发布通知所有关注者/SKU 下架通知），（ 表驱动）；改  构造器注入两新 mock。顺手修  去掉恒为 0 的  形参（unparam）。
- **门禁全绿**：=0、=0、(notification+cmd+httperrs) 0 issues、=0。

## 邮件系统落地（#110–#118，门禁全绿）
- 用户指令「加入邮件系统吧」：在「仅站内」基础上补齐设计文档 §7 邮件系统（此前暂缓）。
- 通道/类目：enum 加 `EnumNotificationChannelEmail` + `EnumNotificationCategorySystem`；`DefaultPreferences()` 五类目（payment/deposit/auction/listing/system），email 默认仅 payment/deposit/system 开、auction/listing 关；`IsChannelEnabled` 语义：缺通道键→false（不影响旧测试）。
- 配置：`shared/modules/config/email.go`（Email{SMTPHost,SMTPPort,SMTPUsername,SMTPPassword,FromAddress,FromName,UseTLS}，mapstructure EMAIL_*）；`Config` 加 `Email` 槽（squash）。
- 端口：`ports/email_port.go`（`EmailMessage{To,Subject,HTMLBody,TextBody}` + `EmailPort.Send`）；`ports/user_email_resolver.go`（`UserEmailResolver.ResolveEmail(ctx,userID)(string,bool,error)` 跨模块只读 users 表）。
- SMTP 适配：`infra/email/smtp_adapter.go`（`SMTPEmailAdapter`，基于 net/smtp；隐式 TLS 465 `tls.Dial` + STARTTLS 587；config 缺 SMTPHost/FromAddress→`Disabled()`→Send no-op；`buildEmailMessage` 生成 multipart/alternative，text/html 经 quoted-printable，UTF-8 subject 经 mime.QEncoding RFC 2047；`tlsConfig()` 强制 MinVersion=TLS1.2 通过 gosec）；`users.sql` 加 `GetUserEmailByID`；`PostgresUserEmailResolver.ResolveEmail` 走 sqlc，ErrNoRows→("",false,nil)。
- 投递链路：`event/envelope` 加 `SubjectEmailRequested=notification.evt.email.requested` + `EmailRequestedPayload` + `BuildEmailRequestedOutboxEvent`（确定性 event_id）；`outbox` 加 `InsertEmailRequestOutboxEvent ... ON CONFLICT(event_id) DO NOTHING` + `SaveEmailRequest`；`create_email_request_command.go` 以 `eventID=source:user:email` 写 outbox；`notification_application_service.dispatch` 在 in_app 后若 email 偏好开→`dispatchEmail`（解析邮箱→写 mail outbox，失败仅 logger.Error 不阻断 in_app）。
- 消费者：`email_dispatch_consumer.go`（`EmailDispatchConsumer`，durable=notification-email-dispatch，FilterSubject=SubjectNotificationEmailRequested，DeliverAll+AckExplicit，AckWait 30s/MaxDeliver 5；`processEmailRequest` 解码→`ports.EmailMessage{HTMLBody:renderEmailHTML}`→`EmailPort.Send`，成功 Ack 失败 Nak）；`renderEmailHTML` 经 html.EscapeString 防注入。
- NATS 流修正：`SubjectNotificationEvents` 由 `notification.evt.*` 改 `notification.evt.>`（覆盖 email.requested）；新增 `SubjectNotificationUserEvents=notification.evt.*`（SSE hub 过滤）+ `SubjectNotificationEmailRequested`；realtime consumer 改用 `SubjectNotificationUserEvents`。
- 装配：`module.go` 加 `NewPostgresUserEmailResolver`(fx.As UserEmailResolver)/`NewSMTPEmailAdapter`(fx.As EmailPort)/`NewCreateEmailRequestCommand`/`NewEmailDispatchConsumer` + `RegisterNotificationEmailConsumer`(Hook OnStart/OnStop)；`cmd/all.go`+`cmd/auction.go` Invoke 加 `RegisterNotificationEmailConsumer`。`make update-mocks` 生成 MockEmailPort/MockUserEmailResolver/MockNotificationOutboxRepository(SaveEmailRequest)。
- 测试（全 PASS）：smtp_adapter_test（multipart headers/UTF-8 subject 编码）、preference_test（system 类目/长度5/未知渠道改 push）、notification_application_service_test（邮件禁用跳过/解析失败仍落 in_app/未知邮箱跳过/邮件开启入队 mail outbox）、email_dispatch_consumer_internal_test（解码发送/HTML 转义/非法 JSON 报错）；原 3 个 payment 持久化测试（显式无 email 键）移除 expectEmailEnqueued，由正向测试覆盖。
- 门禁（2026-07-21）：`go vet ./...`=0；`go build ./...`=0；`golangci-lint run ./internal/modules/notification/...` 0 issues（修 smtp_adapter：tlsConfig MinVersion + Send 参数改名 `_`）；`go test ./internal/modules/notification/...` 全 PASS；`nilaway --include-pkgs=notification`=0；`make nilaway`(auction 域含共享 nats 包)=0。

## users 模块接入 Casbin RBAC（#119–#124，门禁全绿）
- 用户指令「users模块接入casbin实现RBAC配置」。三项决策（均选推荐）：策略落 **Postgres 表 + 运行时管理 API**；模型 **复用 users.role 作身份**（sub=role，p=角色→资源→操作，非 g 分配）；**全量**替换既有 `RequireRole` 路由 → `RequirePermission`。
- 共享包 `internal/shared/modules/authz`：`model.conf`（内嵌 `//go:embed`；r=sub,obj,act / p=sub,obj,act / matcher `r.sub==p.sub && keyMatch4(r.obj,p.obj) && (r.act==p.act||p.act=='*')`）；`PgxAdapter`（`persist.Adapter`，落 `casbin_rules` 表 ptype/v0..v5 + 唯一索引，LoadPolicy/SavePolicy/AddPolicy/RemovePolicy/RemoveFilteredPolicy，`padRule`/`buildPolicyLine` 处理 6 列）；`NewEnforcer`(pool,*logger.Logger)；`RequirePermission()` 中间件（Enforce(role, r.URL.Path, r.Method)，无 claims→401、deny→403、Enforce err→500）；`authz.Module`(fx.Provide Enforcer+Middleware)。**logger 用项目 `logger.Logger`（zerolog），勿用 slog——slog 不在 fx 图，会导致启动期 missing *slog.Logger**。
- **keyMatch4 关键陷阱（务必记）**：casbin keyMatch4 把 `/*` 替换成 `/.*`（多段匹配），路径参数用 `{id}`（不是 `:id`）；`**` 会生成非法正则 **直接 panic/500**，绝不能写 `**`。seed 用 `/api/v1/*`（admin 通配）、`{id}` 写法。
- 迁移：`000026_create_casbin_rules_table.sql`（casbin_rules + 唯一索引）；`000027_seed_casbin_policies.sql`（admin=`/api/v1/* *`；seller=拍卖创建/start/cancel + SPU/SKU 增改发布下架，`{id}` 写法；bidder=保证金创建/资格）。
- users RBAC 管理 API（admin 专属）：`Enum` 无新增；`domain/errs` 加 `ErrInvalidPolicy`；`application/command/add_policy_command.go`+`remove_policy_command.go`（校验 sub/obj/act 非空→ErrInvalidPolicy，调 `Enforcer.AddPolicy/RemovePolicy`）；`application/query/list_policies_query.go`（`GetPolicy`→`PolicyView`，`minPolicyFields=3` 常量）；`infra/http/dto/rbac.go`(`PolicyRequest`)；`infra/http/chi/handler/rbac_handler.go`；`infra/http/chi/router/rbac_router.go`（`RegisterRBACRoutes` 用 `authn.RequireRole(admin)` 保护，而非 Casbin 自身，便于超级用户修复策略）；`users/module.go` 注册 `RegisterRBACRoutes`。
- 路由改造（4 模块 `RegisterXxxRoutes` 加 `*authz.Middleware` 参数）：user_router（GET /、GET /{id}、PATCH /{id}/role 改 RequirePermission）、auction_router（POST /、PUT /{id}/start、PUT /{id}/cancel）、deposit_router（POST /、GET /eligibility）、listing_router（categories 增改删 + spus/skus 增改发布下架）；bid 等公开/仅 RequireAuth 路由不变。`cmd/all.go`+`cmd/auction.go` 模块列表加 `authz.Module`、Invoke 加 `users.RegisterRBACRoutes`。
- 测试（全 PASS）：`authz/middleware_test.go`（内存 enforcer：admin 通配 200 / seller 拍卖 200 / seller 用户列表 403 / bidder 403 / 无 claims 401）；`authz/adapter_test.go`（PgxAdapter 集成，无 `TEST_DATABASE_URL` 自动 skip）；`users command/rbac_policy_test.go`（Add/Remove 命令 + ErrInvalidPolicy）；`users query/list_policies_query_test.go`。
- 门禁（2026-07-21）：`go vet ./...`=0；`go build ./...`=0；`golangci-lint`（authz+users+auction+deposit+listing+cmd）0 issues（修 adapter/enforcer shadow + magic number `minPolicyFields`）；`go test`（上述模块）全 PASS；`make nilaway`(auction 域)=0。
- 后续可选：`.env.example` 无需新增（authz 复用现有 PG 连接池，无独立配置）；可用 testcontainers 补一条端到端「加策略→受保护路由放行/拒绝」集成测试。
